services:
  crondash:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=file:./prisma/dev.db
      - SESSION_SECRET=docker-dev-secret-key-min-32-characters-long
      - NODE_ENV=production
    volumes:
      - crondash-data:/app/prisma

volumes:
  crondash-data:
